import { PageLoading } from '@/components/ui/loading-system';

/**
 * @deprecated Use PageLoading from @/components/ui/loading-system instead
 * This component is kept for backwards compatibility
 */
export function LoadingScreen() {
  return <PageLoading />;
}
